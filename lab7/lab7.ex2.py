import math
n=int(input('введіть кількість рядків: '))
m=int(input('введіть кількість стовпців: '))
def matrix(i,j):
    for i in range(n):
        for j in range(m):
            A = map(lambda j * math.cos(i ** 2 + n))
    return A
result=matrix(i,j)
print(result)

